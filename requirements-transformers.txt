# Pogadane - Transformers (Alternative AI summarization)
# Install: pip install -r requirements-transformers.txt
#
# Use this if you cannot use GGUF models or Ollama
# Set SUMMARY_PROVIDER="transformers" in .config/config.py

transformers>=4.30.0
torch>=2.0.0
accelerate>=0.20.0
sentencepiece>=0.1.99
