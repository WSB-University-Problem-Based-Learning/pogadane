# Pogadane - Production Dependencies
# Install with: pip install -r requirements.txt
#
# This file installs the package with its core dependencies defined in pyproject.toml
# to maintain a single source of truth.
#
# For development: pip install -r requirements-dev.txt
# For testing: pip install -r requirements-test.txt
# For all features: pip install -e .[all]

# Install package with core dependencies
-e .

# Optional: Legacy GUI support (if needed)
# Uncomment to enable ttkbootstrap and customtkinter GUIs:
# -e .[legacy-gui]

# Optional: Transformers backend for local AI
# Uncomment to use SUMMARY_PROVIDER="transformers"
# -e .[transformers]
