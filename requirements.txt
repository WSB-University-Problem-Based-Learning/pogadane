# Pogadane - Production Dependencies
# Install with: pip install -r requirements.txt
#
# This file installs the package with its core dependencies defined in pyproject.toml
# to maintain a single source of truth.
#
# For development: pip install -r requirements-dev.txt
# For testing: pip install -r requirements-test.txt

# Install package with core dependencies
-e .

# Recommended: Faster-Whisper for transcription (4x faster than openai-whisper)
# Uncomment to use TRANSCRIPTION_PROVIDER="faster-whisper" (default)
# -e .[faster-whisper]

# Alternative: OpenAI Whisper for transcription
# Uncomment to use TRANSCRIPTION_PROVIDER="whisper"
# -e .[whisper]

# Optional: Transformers backend for local AI summarization
# Uncomment to use SUMMARY_PROVIDER="transformers"
# -e .[transformers]
