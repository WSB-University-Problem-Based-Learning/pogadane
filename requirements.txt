# Pogadane - Python Dependencies
# Install with: pip install -r requirements.txt

# GUI Framework
ttkbootstrap>=1.10.0

# Google Gemini API client (optional - only needed if using Google instead of Ollama)
google-generativeai>=0.3.0
