# ============================================================================
# Pogadane - Core Dependencies
# ============================================================================
# Install with: pip install -r requirements.txt
#
# This installs the CORE ONLY dependencies (GUI, Google Gemini API, yt-dlp)
# 
# RECOMMENDED STACK (install after core):
#   pip install -r requirements-faster-whisper.txt  # Transcription (4x faster)
#
# The guided installer (python install.py) automatically sets up:
#   - Core dependencies (this file)
#   - faster-whisper (recommended transcription)
#   - llama-cpp-python (GGUF model support)
#   - yt-dlp (YouTube downloader)
#
# OPTIONAL ALTERNATIVES:
#   pip install -r requirements-whisper.txt         # Alternative transcription
#   pip install -r requirements-transformers.txt    # Alternative AI backend
#
# DEVELOPMENT:
#   pip install -r requirements-dev.txt             # Development tools
#   pip install -r requirements-test.txt            # Testing tools
# ============================================================================

# Install package with core dependencies only
-e .
